# 模块化约束

本文规定本项目何时、如何把代码拆成新模块文件，避免「主程序/单个模块越长越臃肿」。
配合 [ARCHITECTURE.md](ARCHITECTURE.md)（现有模块划分）一起读。新增/改动代码前对照本文。

## 1. 分层原则（先分清职责，再谈拆文件）

后端 `src-tauri/src/`：
- **编排/装配层**：`lib.rs`（Builder、命令注册、AppState 装配）、`main.rs`（入口）。
  只做"接线"，**不写业务逻辑**。
- **命令薄封装层**：`commands/<域>.rs` 里的 `#[tauri::command]`。只做参数取用、调状态、调纯逻辑、
  记日志、组织返回。**不实现算法/解析/IO 细节**。
- **纯逻辑层**：顶层模块（`ssh/`、`sshconfig.rs`、`vscode.rs`、`portcheck.rs`、`store.rs`、
  `validate.rs`、`terminal.rs`…）。可独立测试、与 Tauri 解耦。

前端 `frontend/src/`：
- **编排层**：`App.tsx`。数据加载/轮询、所有动作与弹窗状态编排、主题。允许较长，但**只编排**。
- **展示层**：`pages/*`、`components/*`（含 `dialogs.tsx`、`ui/*`）。通过 props 收数据与回调，
  **保持纯展示**，不直接发 IPC、不持有跨页状态。
- **共享层**：`api.ts`（IPC 调用）、`types.ts`（类型）、`i18n.ts`（文案）。

> 黄金规则：一个 `#[tauri::command]` 文件或一个 React 组件里开始出现"解析/编码/查注册表/读文件/拼 SSH 参数"
> 这类纯逻辑时，把逻辑抽到纯逻辑模块，命令/组件只调用它。`vscode.rs`（纯逻辑）↔ `commands/vscode.rs`
> （薄封装）就是范例。

## 2. 何时该开一个新模块文件（触发条件）

满足**任意一条**就考虑拆：

1. **行数超阈值**（经验值，超过即审视，不是硬上限）：
   - 后端纯逻辑模块 / 前端组件 > **300 行**；
   - `dialogs.tsx` 这类聚合文件 > **500 行**；
   - 编排型文件（`App.tsx`、`lib.rs`）放宽，但一旦塞进**业务逻辑**就立刻拆，不看行数。
2. **单文件承担 >1 个清晰职责**：描述它要用"……**和**……""顺便还……"。
3. **新增的是一块自洽能力**：有自己的数据来源/外部依赖/失败模式（如"读 VS Code 历史并打开"
   → 独立 `vscode.rs`，而不是塞进 `terminal.rs` 或 `exec.rs`）。
4. **逻辑被多个命令/组件复用**：抽到共享模块，避免复制粘贴。
5. **薄封装层变厚**：`commands/<域>.rs` 里某命令函数体超过 ~40 行纯逻辑 → 下沉到纯逻辑模块。
6. **一个目录里同类子模块 ≥3 个**：升级为目录 + `mod.rs`（参照 `ssh/`）。

## 3. 怎么拆（放置与命名约定）

后端：
- 纯逻辑：新增**顶层** `src-tauri/src/<域>.rs`；在 `lib.rs` 加 `mod <域>;`。
- 命令：新增 `src-tauri/src/commands/<域>.rs`；在 `commands/mod.rs` 加 `pub mod <域>;`，
  在 `lib.rs` 的 `generate_handler!` 注册。
- 命令文件名与纯逻辑模块同名（`vscode.rs` ↔ `commands/vscode.rs`），一眼对应。
- 子模块多了：建目录 `<域>/`，用 `mod.rs` 再导出（如 `ssh/{command,process,probe,keys,…}`）。
- 跨域复用的小工具进 `util.rs`；数据模型进 `model.rs`。

前端：
- 编排逻辑**留在** `App.tsx`，只把**展示**拆成 `pages/`、`components/`。
- 弹窗集中在 `components/dialogs.tsx`；当它过大（>500 行）再按域拆成 `components/dialogs/<域>.tsx`。
- 新 IPC 一律加在 `api.ts`，类型加在 `types.ts`，文案**中英都加**在 `i18n.ts`。
- 组件内部的小局部组件（如 `HostCard` 内的拆分按钮）可就地定义，不必单开文件，直到被复用。

## 4. 何时**不要**拆（避免过度碎片化）

- 只在一处用、< ~50 行、且不是自洽能力：就地放着。
- 拆完需要在两三个文件间来回跳才能读懂一个流程：说明拆错了边界。
- 只为"看起来整洁"而拆：不拆。可读性优先于文件数量。
- 把编排状态从 `App.tsx` 硬拆出去导致 props 层层透传：宁可留在编排层。

## 5. 提交前自检清单

- [ ] 新逻辑放进了**纯逻辑层**，命令/组件只做薄封装/展示？
- [ ] `lib.rs` / `App.tsx` 里没有新增业务逻辑（只有接线/编排）？
- [ ] 命令文件与纯逻辑模块**同名对应**，并已在 `commands/mod.rs` + `lib.rs` 注册？
- [ ] 有没有文件越过第 2 节阈值且职责不单一？越过就拆。
- [ ] 新 IPC/类型/文案分别落在 `api.ts` / `types.ts` / `i18n.ts`（中英齐全）？
- [ ] 同步更新了 [ARCHITECTURE.md](ARCHITECTURE.md) 的模块列表/命令表？
